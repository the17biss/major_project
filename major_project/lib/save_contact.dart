import 'package:flutter/material.dart';

class SaveContact extends StatelessWidget {
  const SaveContact({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Save Contact"),
      ),
      body: (const Text("Save Contact")),
    );
  }
}
